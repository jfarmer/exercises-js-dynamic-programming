/**
 * Given a set of numbers and a target number, return true if there is a subset
 * of the input set that sums to the target number and false otherwise.
 */
function subsetSum(numbers, target) {
}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = subsetSum;
