/**
 * Given three strings, return true if the third string is
 * an interleaving of the first two and false otherwise.
 *
 * @example
 * isInterleaving('abc', 'xyz', 'axybcz') // => true
 * @example
 * isInterleaving('abc', 'xyz', 'abdecf') // => false
 */
function isInterleaving(left, right, str) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = isInterleaving;
