/**
 * Given a fence with n posts and k colors, return the number of ways of
 * painting the fence such that at most 2 adjacent posts have the same color.
 */

function paintFence(numPosts, numColors) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = paintFence;
