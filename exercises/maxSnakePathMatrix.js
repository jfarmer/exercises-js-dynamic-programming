/**
 * Given a square matrix, find the longest "snake path" in the matrix.
 * A snake path is a sequence of numbers where each new number can only
 * be located to the right or below the previous number, and which differs
 * from the previous number by no more than 1.
 */

function maxSnakePathMatrix(matrix) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = maxSnakePathMatrix;
