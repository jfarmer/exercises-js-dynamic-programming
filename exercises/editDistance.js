/**
 * Given two strings, return their edit distance aka Levinshtein distance.
 *
 * The Levenshtein is the minimum number of single-character edits needed
 * to transform one string into the other. "Single-character edits" means
 * insertions, deletions, or substitutions of a single character.
 *
 * @example
 * editDistance('form', 'fork'); // => 1 (substitute m for k)
 *
 * @example
 * editDistance('kitten', 'sitting'); // => 3 (kitten => sitten => sittin => sitting)
 *
 * @example
 * editDistance('hello', 'help'); // => 2 (hello => hell => help)
 */
function editDistance(left, right) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = editDistance;
