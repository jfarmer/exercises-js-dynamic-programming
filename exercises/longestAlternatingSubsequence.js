/**
 * Given an array of numbers, return the length of the long subsequence
 * whose elements alternate in size.
 *
 * @example
 * // Returns 4 because [5, 9, 8, 2] is longest alternating subsequence
 * longestAlternatingSubsequence([2, 5, 9, 8, 2, 1]);
 */
function longestAlternatingSubsequence(array) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = longestAlternatingSubsequence;
