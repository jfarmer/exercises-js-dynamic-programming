/**
 * Given a string and an array of valid words, determine whether the string
 * can be segmented into a sequence of valid words.
 *
 * @example
 * wordBreak('onetwothree', ['net', 'on', 'one', 'two', 'three']); // => true
 *
 * @example
 * wordBreak('hellofriends', ['hello', 'friend']); // => false
 */
function wordBreak(string, validWords) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = wordBreak;
