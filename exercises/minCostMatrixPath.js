/**
 * Given a matrix, find the minimum cost path from the top left to the bottom right.
 * You can only move right and down. The cost of a path is the sum of the numbers
 * along that path.
 */

function minCostMatrixPath(matrix) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = minCostMatrixPath;
