/**
 * Given an array, return the length of the longest subsequence which
 * occurs at least twice in that array.
 */

function longestRepeatedSubsequence(array) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = longestRepeatedSubsequence;
