/**
 * Given two arrays, return the length of the longest subarray
 * they have in common. Remember, subarrays have to be contiguous.
 *
 * @example
 * // returns 3 because ['D', 'C', 'B'] is
 * // the longest common subarray
 * longestCommonSubsequence(
 *  ['A', 'C', 'D', 'C', 'B', 'C', 'A'],
 *  ['B', 'D', 'C', 'B', 'E', 'D']
 * );
 */
function longestCommonSubarray(left, right) {

}

if (require.main === module) {
  console.log('Running sanity checks:');

  // Add some sanity checks here
}

module.exports = longestCommonSubarray;
